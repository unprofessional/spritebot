version: '3.9'

services:
  soulbot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: soulbot
    env_file:
      - .env
    restart: unless-stopped
    network_mode: bridge
